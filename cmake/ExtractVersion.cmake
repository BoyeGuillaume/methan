function(extract_version _VERSION _NAME)
    string(REPLACE "." ";" VERSION_LIST ${_VERSION})
    list(GET VERSION_LIST 0 _VERSION_MAJOR)
    list(GET VERSION_LIST 1 _VERSION_MINOR)
    list(GET VERSION_LIST 2 _VERSION_PATCH)
    set(_VERSION_STR "${_VERSION_MAJOR}.${_VERSION_MINOR}.${_VERSION_PATCH}")

    set(${_NAME}_VERSION ${_VERSION_STR} PARENT_SCOPE)
    set(${_NAME}_VERSION_MAJOR ${_VERSION_MAJOR} PARENT_SCOPE)
    set(${_NAME}_VERSION_MINOR ${_VERSION_MINOR} PARENT_SCOPE)
    set(${_NAME}_VERSION_PATCH ${_VERSION_PATCH} PARENT_SCOPE)
endfunction()